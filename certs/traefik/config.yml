tls:
  certificates:
    - certFile: /certs/cert.crt
      keyFile: /certs/cert.key
      stores:
        - default
  stores:
    default:
      defaultCertificate:
        certFile: /certs/cert.crt
        keyFile: /certs/cert.key